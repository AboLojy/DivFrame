import { DivFrameDirective } from './div-frame.directive';

describe('DivFrameDirective', () => {
  it('should create an instance', () => {
    const directive = new DivFrameDirective();
    expect(directive).toBeTruthy();
  });
});
